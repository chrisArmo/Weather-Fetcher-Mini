<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <a class="navbar-brand" href="/">Weather</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link {{ isActive "Weather" title  }}" href="/">
            Weather 
            {{#if (isCurrent "Weather" title) }}
            <span class="sr-only">(current)</span>
            {{/if}}
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ isActive "About" title  }}" href="/about">
            About
            {{#if (isCurrent "About" title) }}
            <span class="sr-only">(current)</span>
            {{/if}}
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ isActive "Help" title  }}" href="/help">
            Help
            {{#if (isCurrent "Help" title) }}
            <span class="sr-only">(current)</span>
            {{/if}}
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>